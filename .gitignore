
# 운영 환경 변수 (실제 값)
.env.prod
deploy/nginx/certs/

# 예시 파일은 커밋한다 (전역 gitignore 의 .env.* 규칙보다 우선)
# 운영 예시는 커밋 훅 규칙(.env 로 시작하는 파일 차단)을 피해 deploy/env.prod.example 로 둔다
!ai-engine/.env.example
!web-client/.env.example
